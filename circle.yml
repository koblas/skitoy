general:
    artifacts:
        - "public/"

checkout:
    post:
        - git submodule sync --recursive
        - git submodule update --init --recursive

dependencies:
    override:
        - go get -v github.com/spf13/hugo

test:
    override:
        - hugo
